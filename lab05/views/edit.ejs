<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chỉnh sửa sản phẩm</title>
  <style>
    .form-group {
      margin-bottom: 16px;
    }
    label {
      display: block;
      margin-bottom: 6px;
      color: #555;
    }
    input[type="text"],
    input[type="number"],
    input[type="file"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #e6e9ef;
      border-radius: 8px;
    }
    .current-image {
      text-align: center;
      margin-top: 10px;
    }
    .current-image img {
      max-width: 200px;
      border-radius: 6px;
      border: 1px solid #eaeef6;
    }
    button.submit-btn {
      width: 100%;
      padding: 12px;
      border-radius: 8px;
      background: var(--accent);
      color: #fff;
      border: none;
      margin-top: 8px;
    }
    .back-link {
      display: block;
      text-align: center;
      margin-top: 14px;
      color: var(--accent);
      text-decoration: none;
    }
  </style>
</head>
<body>
<%- include('partials/header', { title: 'Chỉnh sửa sản phẩm' }) %>
<section class="form-grid">
  <style>
    .form-group{margin-bottom:20px}
    label{display:block;margin-bottom:8px;color:var(--text);font-weight:600;font-size:14px}
    input[type="text"], input[type="number"], input[type="file"]{
      width:100%;
      padding:14px;
      border:2px solid var(--border);
      border-radius:12px;
      font-size:16px;
      transition:all 0.3s ease;
      background:rgba(255,255,255,0.8);
    }
    input[type="text"]:focus, input[type="number"]:focus{
      outline:none;
      border-color:var(--primary);
      box-shadow:0 0 0 3px rgba(102, 126, 234, 0.1);
      transform:translateY(-1px);
    }
    input[type="file"]{
      padding:12px;
      border-style:dashed;
      background:rgba(102, 126, 234, 0.05);
    }
    .current-image{
      text-align:center;
      margin-top:16px;
      padding:20px;
      background:rgba(102, 126, 234, 0.05);
      border-radius:12px;
      border:2px dashed var(--primary);
    }
    .current-image img{
      max-width:200px;
      border-radius:12px;
      box-shadow:0 8px 25px rgba(102, 126, 234, 0.3);
      transition:all 0.3s ease;
    }
    .current-image img:hover{
      transform:scale(1.05);
    }
    button.submit-btn{
      width:100%;
      padding:16px;
      border-radius:12px;
      background:linear-gradient(45deg, var(--accent), var(--accent-dark));
      color:#fff;
      border:none;
      margin-top:16px;
      font-weight:600;
      font-size:16px;
      transition:all 0.3s ease;
      box-shadow:0 4px 15px rgba(240, 147, 251, 0.4);
      text-transform:uppercase;
      letter-spacing:1px;
    }
    button.submit-btn:hover{
      transform:translateY(-2px);
      box-shadow:0 8px 25px rgba(240, 147, 251, 0.6);
    }
    .back-link{
      display:block;
      text-align:center;
      margin-top:20px;
      color:var(--accent);
      text-decoration:none;
      font-weight:600;
      transition:all 0.3s ease;
    }
    .back-link:hover{
      color:var(--accent-dark);
      transform:translateY(-1px);
    }
  </style>

  <h1 style="text-align:center;margin-bottom:32px;">✏️ Chỉnh sửa sản phẩm</h1>
  <form action="/edit/<%= product.id %>" method="post" enctype="multipart/form-data">
    <div class="form-group">
      <label for="name">📝 Tên sản phẩm:</label>
      <input type="text" id="name" name="name" value="<%= product.name %>" placeholder="Nhập tên sản phẩm..." required>
    </div>
    <div class="form-group">
      <label for="price">💰 Giá:</label>
      <input type="number" id="price" name="price" value="<%= product.price %>" placeholder="Nhập giá sản phẩm..." required>
    </div>
    <div class="form-group">
      <label for="image">📷 Chọn ảnh mới:</label>
      <input type="file" id="image" name="image" accept="image/*">
      <div class="current-image">
        <% if (product.url_image) { %>
          <img src="<%= product.url_image %>" alt="<%= product.name %>">
          <p style="margin-top:12px;color:var(--text-light);font-size:14px">🖼️ Ảnh hiện tại: <%= product.url_image %></p>
        <% } else { %>
          <p style="color:var(--text-light)">📷 Chưa có ảnh</p>
        <% } %>
      </div>
    </div>
    <button type="submit" class="submit-btn">💾 Cập nhật</button>
  </form>
  <a href="/" class="back-link">← 🔙 Quay lại danh sách</a>
</section>
<%- include('partials/footer') %>
</body>
</html>
